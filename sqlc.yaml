version: "2"
sql:
  - schema: "sql/schema" # Where to find CREATE TABLE statements
    queries: "sql/queries" # Where to find your custom SQL queries
    engine: "postgresql" # The type of database
    gen:
      go:
        out: "internal/database" # Where to output the generated Go code
